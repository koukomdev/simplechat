<header>
    <div class="navbar navbar-dark bg-dark box-shadow">
        <div class="container d-flex justify-content-between">
            <a href="/" class="navbar-brand d-flex align-items-center">
                <strong>simplechat</strong>
            </a>
            <% if session[:user_id] %>
                <strong>
                    <span class="navbar-brand">　<i class="fa fa-user"></i> <%= session[:username] %></span> 
                    <a href="/signout" class="navbar-brand"> <i class="fa fa-sign-out"></i> </a>
                </strong>
            <% else %>
                <strong class="navbar-brand">
                    <a href="/signin" class="navbar-brand">ログイン <i class="fa fa-sign-in"></i> </a>
                </strong>
            <% end %>
        </div>
    </div>
</header>
